import Color from '../constants/colors';

const customContentOptions = {
  activeTintColor: Color.primary,
  inactiveTintColor: '#ddd',
  labelStyle: { fontSize: 25 },
  itemStyle: {
    elevation: 1,
    //backgroundColor: '#111',
  },
};

export default customContentOptions;
